import 'package:flutter/material.dart';

class RiskMapPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFF98B1BF),
      appBar: AppBar(title: Text("风险地图")),
      body: Center(
        child: Text("地图显示（待实现）"),
      ),
    );
  }
}
